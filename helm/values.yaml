replicaCount: 1
# v1.2.0-660ae95
image:
  registry: "ghcr.io"
  repository: "ghcr.io/vit-um/kbot"
  tag: v1.2.0-660ae95
  os: linux
  arch: amd64
# Додатково визначте секцію для токену TELE_TOKEN
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
