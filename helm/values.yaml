replicaCount: 1
image:
  repository: umanetsvitaliy
  tag: v1.4.1-c5c88e1

# Додатково визначте секцію для токену TELE_TOKEN
secret:
  name: "TELE_TOKEN"
  tokenKey: "token"
  tokenName: "kbot"
securityContext:
  privileged: true
